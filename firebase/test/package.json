{
  "name": "duren-firestore-rules-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 0 smoke test for Duren Firestore Security Rules.",
  "scripts": {
    "test": "mocha --timeout 10000 profiles.rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "mocha": "^10.7.3"
  }
}
