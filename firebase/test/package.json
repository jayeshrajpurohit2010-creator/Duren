{
  "name": "duren-firestore-rules-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Firestore Security Rules tests for Duren (profiles, embers, lanterns, tribes).",
  "scripts": {
    "test": "mocha --timeout 10000 \"*.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "mocha": "^11.7.6"
  }
}
